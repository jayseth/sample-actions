name:  Sonar CodeQL JIRA

on:
  workflow_dispatch:

jobs:
  SonarScan: 
    uses: ./.github/workflows/Dotnet_Build_Sonar_Scan.yml
    secrets: inherit

  CodeQL: 
    uses: ./.github/workflows/codeql.yml
    secrets: inherit

  SonarJIRA:
        needs: SonarScan
        uses: ./.github/workflows/Sonar_Python_Jira.yml
        secrets: inherit
